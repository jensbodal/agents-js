# Test agent notes

Review-time rule: humans review code much slower than agents generate it;
estimate review time, not generation time.

This file exists so the trial-agent fixture has a workspace memory entry
that matches the default probe query "memory rule about time estimates".
