---
name: valid-skill
description: Perform the same role as the primary valid-skill fixture but live in an alternate search path so tests can assert precedence rules (first search path wins). The body text here is different from the primary fixture so a test can tell which copy was loaded.
---

# Valid Skill (Alt)

This is the alternate-path copy. A test should never see this when the
primary search path is listed first.
