---
name: only-in-alt
description: Perform a fallback role by existing only in the alternate search path. Tests use this to assert that discovery walks every search path, not just the first one, when resolving names that do not appear in the primary path.
---

# Only In Alt

Lives only in the alternate path. Should still be findable via listSkills
when both paths are provided.
