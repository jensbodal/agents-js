/**
 * Shared constants for `@agents-js/a2ui-renderer`.
 */

/**
 * Surface id used by {@link renderA2uiComponent} when callers omit an
 * explicit `surfaceId`. Hosts that render a single surface can rely on
 * this value when wiring up their action-event handlers.
 */
export const DEFAULT_SURFACE_ID = "default";
