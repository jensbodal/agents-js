# @agents-js/plane

> Plane (project management) webhook handler. HMAC-verified fetch handler for Plane issue events with pluggable notifier transport.

## Installation

```sh
bun add @agents-js/plane
```

## API

<!-- Auto-generated from JSDoc -->

### Functions

- **`createPlaneWebhookFetchHandler`** — Env-driven Plane webhook mount used by the CLI's `serve` command and `apps/internal-gateway`. Wires the core webhook handler with: - secret resolution via {resolvePlaneWebhookSecretFromEnv} (env, t...

### Interfaces

- **`PlaneWebhookFetchHandlerOptions`**
- **`Logger`** — Shared logger contract for `-js/plane`. Mirrors the `console` shape so a bare `console` satisfies it without adapter code.

### Exports

- **`verifyPlaneSignature`**
- **`resolvePlaneWebhookSecretFromEnv`**


## Dependencies

- `@agents-js/gateway-runtime`

## License

MIT

<!-- AUTO-GENERATED by scripts/generate-package-readmes.ts — do not edit -->
