{
  "name": "@agents-js/example-renderer-adapter-prototype",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Slot-2 host-local Canvas2D-style prototype proving the RendererAdapter lifecycle types from @agents-js/a2ui-host (ADR-0009 slot-1). Demonstrates mount/apply/resize/capture/stats/dispose ordering against a mock canvas target.",
  "scripts": {
    "test": "bun test tests",
    "typecheck": "bunx tsgo -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agents-js/a2ui-host": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "catalog:"
  }
}
