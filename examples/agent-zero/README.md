# Agent Zero Examples

Reference files for working with Agent Zero from this repository. This folder
is intentionally outside the Bun workspace and is not exported as an
`@agents-js/*` package.

Run the local source consumer with:

```sh
bun examples/agent-zero/a2a-client-local-source-consumer.ts [url] [prompt]
```
