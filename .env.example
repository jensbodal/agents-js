# ACP runtime harness to use (required). Valid values: opencode, claude
AJS_DEFAULT_HARNESS=pi

# Default LLM model ID applied when a session starts (optional).
# Must match a model ID advertised by the runtime.
# AJS_DEFAULT_MODEL=
